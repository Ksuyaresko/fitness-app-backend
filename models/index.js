const { User, schemas: schemasUser } = require("./users");
const { Product } = require("./product");
const { Diary } = require("./diary-exercise-schema");

module.exports = { User, schemasUser, Product, Diary };
