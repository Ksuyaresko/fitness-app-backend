const { User, schemas: schemasUser } = require("./users");
const { Product } = require("./products");

const {Exercise} = require('./exercises')


module.exports = { User, schemasUser, Product, Exercise };

